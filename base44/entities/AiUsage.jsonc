{
  "name": "AiUsage",
  "type": "object",
  "properties": {
    "window_key": {
      "type": "string",
      "description": "UTC hourly rate-limit bucket"
    },
    "request_count": {
      "type": "integer",
      "default": 0
    },
    "last_request_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "window_key",
    "request_count"
  ],
  "rls": {
    "create": true,
    "read": {
      "created_by": "{{user.email}}"
    },
    "update": {
      "created_by": "{{user.email}}"
    },
    "delete": {
      "created_by": "{{user.email}}"
    }
  }
}