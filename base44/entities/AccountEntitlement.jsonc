{
  "name": "AccountEntitlement",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string",
      "description": "Base44 user ID receiving this entitlement"
    },
    "user_email": {
      "type": "string",
      "format": "email"
    },
    "plan": {
      "type": "string",
      "enum": [
        "free",
        "builder",
        "pro",
        "agency"
      ],
      "default": "free"
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "trialing",
        "past_due",
        "canceled",
        "inactive"
      ],
      "default": "active"
    },
    "billing_provider": {
      "type": "string",
      "enum": [
        "none",
        "stripe",
        "google_play"
      ],
      "default": "none"
    },
    "provider_customer_id": {
      "type": "string"
    },
    "provider_subscription_id": {
      "type": "string"
    },
    "current_period_end": {
      "type": "string",
      "format": "date-time"
    },
    "cancel_at_period_end": {
      "type": "boolean",
      "default": false
    },
    "ai_hourly_limit": {
      "type": "integer",
      "minimum": 1,
      "default": 5
    },
    "ai_monthly_limit": {
      "type": "integer",
      "minimum": 1,
      "default": 10,
      "description": "Included AI generations per UTC calendar month"
    },
    "bonus_ai_credits": {
      "type": "integer",
      "minimum": 0,
      "default": 0,
      "description": "Purchased IABT credits; consumed after the monthly allowance and usable for weighted paid-media production"
    },
    "ai_usage_hour": {
      "type": "string",
      "description": "Current UTC hourly quota bucket"
    },
    "ai_hourly_used": {
      "type": "integer",
      "minimum": 0,
      "default": 0,
      "description": "Number of generation requests reserved in the current UTC hour"
    },
    "ai_usage_month": {
      "type": "string",
      "description": "Current UTC calendar-month quota bucket"
    },
    "ai_monthly_used": {
      "type": "integer",
      "minimum": 0,
      "default": 0,
      "description": "Included IABT credits reserved in the current UTC calendar month"
    },
    "project_limit": {
      "type": "integer",
      "minimum": 0,
      "default": 1,
      "description": "0 means unlimited"
    },
    "static_zip_export_enabled": {
      "type": "boolean",
      "default": false
    },
    "react_export_enabled": {
      "type": "boolean",
      "default": false
    },
    "commercial_use_enabled": {
      "type": "boolean",
      "default": false
    },
    "white_label_exports_enabled": {
      "type": "boolean",
      "default": false
    },
    "team_seat_limit": {
      "type": "integer",
      "minimum": 1,
      "default": 1
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "user_id",
    "user_email",
    "plan",
    "status",
    "billing_provider",
    "ai_hourly_limit"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}